[project]
name = "filmtracker-api"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlmodel",
    "alembic"
    ,"uuid6"
]

[project.optional-dependencies]
test = ["pytest", "httpx"]
